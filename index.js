'use strict';
const shell = require('shelljs');

console.log('The nodejs script ran');

// process.stdin.on('data', function (chunk) {
//   process.stdout.write('data: ' + chunk);
// }).resume();

// shell.exec('./input.sh');

